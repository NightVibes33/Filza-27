name: Deprecated - universal iOS 16 IPA removed

on:
  workflow_dispatch:

jobs:
  removed:
    runs-on: ubuntu-latest
    steps:
      - name: Universal iOS 16 package is removed
        run: |
          echo "The iOS 16.1 split-runtime/universal IPA path has been removed."
          echo "Filza 27 now ships the integrated modern iOS 17+ runtime only."
