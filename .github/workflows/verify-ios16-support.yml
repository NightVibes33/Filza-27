name: Deprecated - iOS 16 support removed

on:
  workflow_dispatch:

jobs:
  removed:
    runs-on: ubuntu-latest
    steps:
      - name: iOS 16 support is removed
        run: |
          echo "Filza 27 no longer builds or publishes an iOS 16 compatibility runtime."
          echo "Use the modern iOS 17+ verification workflow instead."
